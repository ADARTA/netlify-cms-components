{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "cd packages/netlify-cms-starter && npm run start",
    "serve:build": "serve -s packages/netlify-cms-starter/build",
    "build": "lerna run build",
    "build:starter": "cd packages/netlify-cms-starter && npm run build"
  },
  "devDependencies": {
    "lerna": "^3.8.0"
  }
}
